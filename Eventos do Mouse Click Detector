ClickDetector.MouseClick:Connect(function(player)
    -- código a ser executado quando o jogador clicar na parte com o botão esquerdo do mouse
end)

ClickDetector.MouseHoverEnter:Connect(function(player)
    -- código a ser executado quando o mouse do jogador entrar na área do ClickDetector
end)

ClickDetector.MouseHoverLeave:Connect(function(player)
    -- código a ser executado quando o mouse do jogador sair da área do ClickDetector
end)

ClickDetector.RightMouseClick:Connect(function(player)
    -- código a ser executado quando o jogador pressionar o botão direito do mouse
end)
